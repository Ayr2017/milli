pub struct DataSource {
    pub name: String,
    pub host: String,
    pub database: String,
    pub username: String,
    pub password: String,
    pub port: u16,
    pub database_path: String,
    pub database_name: String,
    pub database_type: String,
}